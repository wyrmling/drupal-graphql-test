{# {{ attach_library('sw_theme/global') }} #}
<main class="poc-page">
  <h1 class="poc-page__title">Pokemon</h1>

  {% if error %}
    <div class="poc-alert poc-alert--error" role="alert">
      <strong>Error:</strong> {{ error }}
    </div>
  {% elseif items is empty %}
    <div class="poc-alert poc-alert--empty" role="status">
      No Pokemon found.
    </div>
  {% else %}
    <ul class="poc-grid">
      {% for pokemon in items %}
        {%
          set types = pokemon.pokemon_v2_pokemontypes
            | map(t => t.pokemon_v2_type.name)
            | join(', ')
        %}
        <li class="poc-card">
          <span class="poc-card__id">#{{ pokemon.id }}</span>
          <h2 class="poc-card__name">{{ pokemon.name | capitalize }}</h2>
          <dl class="poc-card__meta">
            <dt>Type</dt>  <dd>{{ types }}</dd>
            <dt>Height</dt><dd>{{ pokemon.height }}</dd>
            <dt>Weight</dt><dd>{{ pokemon.weight }}</dd>
            <dt>XP</dt>    <dd>{{ pokemon.base_experience }}</dd>
          </dl>
          <a class="poc-btn" href="{{ path('sw_graphql.detail', {id: pokemon.id}) }}">
            View Details
          </a>
        </li>
      {% endfor %}
    </ul>

    {% if total_pages > 1 %}
      <nav class="poc-pagination" aria-label="Pagination">
        {% if page > 0 %}
          <a class="poc-pagination__btn" href="{{ path('sw_graphql.list') }}?page={{ page - 1 }}">← Prev</a>
        {% endif %}
        <span class="poc-pagination__info">Page {{ page + 1 }} of {{ total_pages }}</span>
        {% if page + 1 < total_pages %}
          <a class="poc-pagination__btn" href="{{ path('sw_graphql.list') }}?page={{ page + 1 }}">Next →</a>
        {% endif %}
      </nav>
    {% endif %}
  {% endif %}
</main>
