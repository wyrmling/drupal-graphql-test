{# {{ attach_library('sw_theme/global') }} #}
<main class="poc-page">
  <a class="poc-back-link" href="{{ path('sw_graphql.list') }}">← Back to list</a>

  {% if error %}
    <div class="poc-alert poc-alert--error" role="alert">
      <strong>Error:</strong> {{ error }}
    </div>
  {% elseif pokemon is empty %}
    <div class="poc-alert poc-alert--empty" role="status">
      Pokemon not found.
    </div>
  {% else %}
    {%
      set sprites_raw = pokemon.pokemon_v2_pokemonsprites[0].sprites ?? '{}'
    %}
    {%
      set sprite_url = sprites_raw['front_default'] ?? null
    %}

    <div class="poc-detail">
      <div class="poc-detail__hero">
        {% if sprite_url %}
          <img class="poc-detail__sprite" src="{{ sprite_url }}" alt="{{ pokemon.name | capitalize }} sprite" loading="lazy">
        {% endif %}
        <div class="poc-detail__header">
          <span class="poc-card__id">#{{ pokemon.id }}</span>
          <h1 class="poc-detail__name">{{ pokemon.name | capitalize }}</h1>
          <div class="poc-detail__types">
            {% for t in pokemon.pokemon_v2_pokemontypes %}
              <span class="poc-badge poc-badge--{{ t.pokemon_v2_type.name }}">{{ t.pokemon_v2_type.name }}</span>
            {% endfor %}
          </div>
        </div>
      </div>

      <section class="poc-detail__section">
        <h2>Base Info</h2>
        <dl class="poc-dl">
          <dt>Height</dt>     <dd>{{ pokemon.height }}</dd>
          <dt>Weight</dt>     <dd>{{ pokemon.weight }}</dd>
          <dt>Base XP</dt>    <dd>{{ pokemon.base_experience }}</dd>
        </dl>
      </section>

      <section class="poc-detail__section">
        <h2>Abilities</h2>
        <ul class="poc-tag-list">
          {% for a in pokemon.pokemon_v2_pokemonabilities %}
            <li>{{ a.pokemon_v2_ability.name }}</li>
          {% endfor %}
        </ul>
      </section>

      <section class="poc-detail__section">
        <h2>Base Stats</h2>
        <ul class="poc-stats">
          {% for s in pokemon.pokemon_v2_pokemonstats %}
            <li class="poc-stat">
              <span class="poc-stat__name">{{ s.pokemon_v2_stat.name }}</span>
              <div class="poc-stat__bar-wrap">
                <div class="poc-stat__bar" style="--val: {{ s.base_stat }}"></div>
              </div>
              <span class="poc-stat__val">{{ s.base_stat }}</span>
            </li>
          {% endfor %}
        </ul>
      </section>
    </div>
  {% endif %}
</main>
