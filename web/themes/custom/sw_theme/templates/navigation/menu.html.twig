{% import _self as menus %}

{{ menus.menu_links(items, attributes, 0) }}

{% macro menu_links(items, attributes, menu_level) %}
  {% import _self as menus %}
  {% if items %}
    {% if menu_level == 0 %}
      <ul{{ attributes.addClass('sw-menu') }}>
    {% else %}
      <ul class="sw-menu sw-menu--sub">
    {% endif %}
    {% for item in items %}
      <li{{ item.attributes.addClass('sw-menu__item') }}>
        {{ link(item.title, item.url, { 'class': ['sw-menu__link'] }) }}
        {% if item.below %}
          {{ menus.menu_links(item.below, attributes, menu_level + 1) }}
        {% endif %}
      </li>
    {% endfor %}
    </ul>
  {% endif %}
{% endmacro %}
